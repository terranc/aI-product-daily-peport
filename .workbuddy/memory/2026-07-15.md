# 2026-07-15 工作日志

## 产品标星功能 + 星标页 Modal

- 实现产品标星功能：详情页标星按钮（localStorage 持久化，key `ai_radar_starred`）、顶部导航「星标」入口+数量徽章、starred.html 星标列表页（取消标星、实时同步、分页）
- 星标页点击产品改为 Modal 打开详情（与首页体验一致）：抽取共享常量 `MODAL_CORE_JS`/`MODAL_CSS`/`MODAL_HTML`，事件委托绑定 `.product-item a[href]`，支持 ESC/遮罩/前进后退/深度链接
- 关键技术：Modal 通过 fetch + replaceChildren 动态注入 DOM，注入的 script 不执行，故标星逻辑用 `onclick` 内联 + 注入后 `initStarButtons()`
- 修改 `scripts/build_site.py`，重建 docs/，部署 GitHub Pages
- commit: 8ba851b（标星功能）、db0ae9d（星标页 Modal）

## 星标页卡片点击区 + 导航选中样式调整

- 星标页产品卡片改为整张可点击：`renderProduct` 用 `<a class="product-link">` 包裹缩略图+信息，与首页体验一致；unstar-btn 留在 `<a>` 外并提 `z-index`
- 导航星标选中样式：紫色文字+浅紫背景+紫色内边框（box-shadow inset）+紫色五角星填充，去掉黄色
- commit: 40f50d4

## 自动化日报 2026-07-15

- 抓取 183 个候选（Twitter 50、V2EX 39、HN 29、Reddit 15、PH 50）
- 数据库去重后 180 个候选进入 LLM 筛选
- 精选 TOP 5：VocalVia（文档转多语音播客）、Breva（Mac 触控板呼吸冥想）、DayReplay（AI 自动时间追踪）、ClipFlow（AI 静音移除视频剪辑）、NeuralLen（AI 智能翻译+OCR+PDF）
- 来源渠道：Product Hunt 3、HN 1、V2EX 1
- 5/5 截图全部成功
- 网站已构建并部署到 GitHub Pages（main + gh-pages）
- 产品数据库更新至 257 个产品（新增 5 个）
