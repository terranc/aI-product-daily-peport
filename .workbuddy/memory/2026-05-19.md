# 2026-05-19 工作记录

## AI Product Radar 每周深度分析（自动化任务）

- 运行 `weekly_analysis.py`：数据库共 34 个产品，均为今天首次入库，0 个满足深度分析条件（需 ≥10 天 + 近7天活跃）
- 运行 `build_site.py`：成功生成 3 个产品页
- 部署：`git push origin main` + `gh-pages --force`，成功推送到 GitHub Pages
- 网站地址：https://terranc.github.io/aI-product-daily-peport/

**关键经验**：GVM 会劫持 `git` 命令，需用 `/opt/homebrew/bin/git -C <dir>` 方式调用；Python 脚本需用绝对路径 `/opt/homebrew/bin/python3 /abs/path/script.py`
