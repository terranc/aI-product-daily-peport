# 2026-05-19 工作记录

## AI Product Radar 每周深度分析（自动化任务）

- 运行 `weekly_analysis.py`：数据库共 34 个产品，均为今天首次入库，0 个满足深度分析条件（需 ≥10 天 + 近7天活跃）
- 运行 `build_site.py`：成功生成 3 个产品页
- 部署：`git push origin main` + `gh-pages --force`，成功推送到 GitHub Pages
- 网站地址：https://terranc.github.io/aI-product-daily-peport/

**关键经验**：GVM 会劫持 `git` 命令，需用 `/opt/homebrew/bin/git -C <dir>` 方式调用；Python 脚本需用绝对路径 `/opt/homebrew/bin/python3 /abs/path/script.py`

## AI Product Radar 每日抓取（自动化任务，20:00）

- 抓取：57 个候选产品（HN + GitHub + Reddit）
- 去重后：57 个（数据库中 34 个历史产品 ID 与候选 ID 生成方式不同，未命中冷却期过滤）
- LLM 过滤后通过筛选：~20 个用户产品（排除纯开发/框架/讨论帖）
- 精选 TOP 5：drea(8分)、Lumis(8分)、GetFastVisa(8分)、Clawputer(7分)、Zuzu.club(7分)
- 截图：5 张网站截图 + drea 获取到 App Store 截图（但匹配了错误应用，已清空）
- JSON 踩坑：中文书名号 `"对话"` 导致 JSON 解析失败，改用 `json.dump()` 生成文件可避免
- 构建网站：5 个产品页面，build_site.py 成功
- 部署：main + gh-pages 均推送成功

## AI Product Radar 每日抓取（自动化任务，22:50）

- 抓取：145 个候选产品（HN 30 + Reddit 60 + Product Hunt 15 + V2EX 40）
- 去重后：145 个（数据库 39 个冷却期产品 URL/名称均不匹配）
- LLM 过滤后通过筛选：约 38 个用户产品
- 精选 TOP 5：Saner.AI(9分)、PersonalPathways(8分)、Scripod(8分)、Rtriv(7分)、BillWatch(7分)
- 截图：Saner.AI + Scripod 成功，其余 3 个截图服务返回 HTTP 500
- 构建+部署：5 个产品页，main + gh-pages 推送成功
- 踩坑：raw-candidates.json / products.json 均为 dict 结构（需取 .products 子字段）
